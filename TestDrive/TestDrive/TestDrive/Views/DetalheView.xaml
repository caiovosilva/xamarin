<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TestDrive.Views.DetalheView">
        <StackLayout Padding="25">
            <TableView Intent="Settings">
                <TableRoot>
                    <TableSection Title="Acessórios">
                        <SwitchCell Text="{Binding TextoFreioABS}" On="True" />
                        <SwitchCell Text="{Binding TextoArCondicionado}" />
                        <SwitchCell Text="{Binding TextoMP3}" />
                        <TextCell Text="{Binding TextoTotal}" />
                    </TableSection>
                </TableRoot>
            </TableView>
            
            <Button 
                VerticalOptions="Center"
                HorizontalOptions="Center"
                Text="Próximo"
                x:Name="buttonProximo"
                Clicked="buttonProximo_Clicked" />
        </StackLayout>
</ContentPage>